[Unit]
# thanos-sidecar has Requires=prometheus.service, so stopping Prometheus
# stops the sidecar too - but starting Prometheus did not bring it back.
# This makes the dependency bidirectional.
Wants=thanos-sidecar.service
